
# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# An example controller showing a sample home page
GET      /minefield                           controllers.MinesController.getBoard
POST     /minefield                           controllers.MinesController.createBoard
POST     /minefield/sweep                     controllers.MinesController.createSweep(x: Int, y: Int)

      